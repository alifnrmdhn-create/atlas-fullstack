// Halaman /programs/archived — selalu memakai ProgramsView desktop penuh (named
// export), BUKAN wrapper responsif default (yang mengganti ke ProgramsMobile di
// phone untuk route /programs utama). Archived = view sekunder, tetap tabel penuh.
import { ProgramsView } from '../ProgramsView'

export default ProgramsView
