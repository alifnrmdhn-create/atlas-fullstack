; PHP overrides ATLAS — dimuat via PHP_INI_SCAN_DIR (lihat nixpacks.toml start).
; Audit 2026-06-17: default PHP upload_max_filesize=2M / post_max_size=8M
; menolak foto HP (2-5MB) diam-diam SEBELUM sampai validasi app (10MB) →
; error membingungkan "The files.0 failed to upload". Naikkan agar selaras.
upload_max_filesize = 20M
post_max_size = 30M
